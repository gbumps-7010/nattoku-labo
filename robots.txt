# robots.txt for もう失敗しない。ナットクLabo

# すべてのクローラーに対して
User-agent: *

# すべてのページをクロール許可
Allow: /

# 製品ページを明示的に許可
Allow: /products/
Allow: /products/*

# プライバシーポリシーとAboutページを許可
Allow: /privacy
Allow: /about

# トップは https://nattoku-labo.com/ を正規URLとする（index.html はホスティングで / へ 301）

# 以下のディレクトリはクロール不要（存在する場合）
Disallow: /admin/
Disallow: /private/
Disallow: /temp/
Disallow: /test/
Disallow: /data-templates/
Disallow: /tools/

# 製品JSON・広告断片は _headers の X-Robots-Tag で noindex
# Googleがnoindexを確認できるよう、robots.txt ではブロックしない

# 旧テンプレート・廃止製品は _redirects で 410
# 旧スラッグは 301 で新URLへ（robots でブロックしない＝Googleがリダイレクトを辿れる）
# UTM等付きURLは functions/_middleware.js で正規URLへ301（代替ページ化を防止）

# サイトマップの場所（拡張子なしの正規URLのみ。リダイレクト元は含めない）
Sitemap: https://nattoku-labo.com/sitemap.xml
